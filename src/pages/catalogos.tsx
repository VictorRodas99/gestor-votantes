import EmptyState from '../components/empty-state'

function CatalogosPage() {
  return (
    <EmptyState
      title="Catálogos"
      description="ABM de punteros, referentes, locales y sectores (en construcción)."
    />
  )
}

export default CatalogosPage
