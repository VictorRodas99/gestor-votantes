export type AsignacionPunteroRaw = {
  id: string
  votante_id: string
  puntero_id: string
}

/** Modelo de dominio de una asignación ya casteada. */
export type AsignacionPuntero = {
  id: number
  votanteId: number
  punteroId: number
}
